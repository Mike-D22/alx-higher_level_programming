-- It displays number of records with id = 89 in the table first_table of the dbase hbtn_0c_0 in your MySQL server.
-- The dbase name will be passed as an argument of the mysql command.

SELECT COUNT(*) FROM first_table WHERE id=89;
